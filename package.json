{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "dependencies": {
    "component": "^1.1.0",
    "cypress": "^14.2.0",
    "react-tabs": "^6.1.0",
    "styled": "^1.0.0"
  }
}
